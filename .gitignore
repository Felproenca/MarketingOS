node_modules/
.env
.env.local
data/
.DS_Store
Thumbs.db
.marketingos/

# Dados de clientes — nunca subir
clients/*/
!clients/_template/

# Clones temporários de análise
_tmp_*/

# Memória local do Claude Code
.claude/

# Netlify local state (deployment-specific)
.netlify/
_tmp_*.html

# Sherlock — sessões de navegador (cookies/login, nunca commitar)
.sherlock/

# WhatsApp session e cache
.whatsapp-session/
.wwebjs_cache/
.wwebjs_auth/

# Agency — dados operacionais (leads, demos, contatos são privados)
agency/leads/
agency/demos/
agency/contacted/
agency/instagram-config.json
agency/bot-state.json
agency/outputs/

# Editor
.vscode/
.vercel
